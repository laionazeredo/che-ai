# MANUAL TEST EXECUTION REPORT

> Generated by `harness-manual-test-executor` after running a manual test plan.
> File location: `<WORKTREE_ROOT>/.trae/<task-id>/MANUAL_TEST_EXECUTION_REPORT.md
> Companion files:
>   - Original plan: `.trae/<task-id>/manual_test_plan.md`
>   - Evidence directory: `.trae/<task-id>/manual_test_evidence/`
>   - Execution log: `.trae/<task-id>/manual_test_evidence/execution.log`
> Language: English (per harness rules). Chat summary of this report is in Portuguese.

---

# Manual Test Execution Report — `<task-id-slug>`

- **Started:** `<ISO datetime>`
- **Finished:** `<ISO datetime>`
- **Duration:** `<HHh MMm>`
- **Worktree:** `<absolute path>`
- **Branch / PR:** `<branch name> | <PR URL if applicable>`
- **Target URLs tested:** `<base URL 1>, <base URL 2>`
- **Driver used:** Playwright MCP / HTTP-only / mixed
- **Plan file:** [manual_test_plan.md](<relative path>)

---

## 1. Environment Setup — Result

| Item | Value |
|---|---|
| Setup ran? | YES (A = executed fully) / (B = skipped — app already running) / (C = user cancelled abort) |
| Setup exit code | `0` / non-zero: `<N>` |
| Setup duration | `<Mm>` |
| Notes | `Link to ENVIRONMENT_SETUP.log if ran` |

**Setup PASS / FAIL — `<one line summary: e.g. "All setup commands exited 0, platform@3000 ready in 42s".
Errors, if any:>

---

## 2. Per-Acceptance-Criterion Summary

| AC-ID | Title | Verdict | Severity if FAIL | Evidence dir | Notes |
|---|---|---|---|---|---|
| AC-1 | `<title>` | ✅ / ⚠️ / ❌ / ⏭️ | — / BLOCKER / HIGH / MED / LOW | [AC-1/](<rel path>) | `<1 line>` |
| AC-2 | `<title>` | ✅ / ⚠️ / ❌ / ⏭️ | — / BLOCKER / HIGH / MED / LOW | [AC-2/](<rel path>) | `<1 line>` |
| ... | ... | ... | ... | ... | ... |

**Totals:** `N ACs total — N PASS / N PARTIAL / N FAIL / N SKIP.`

---

## 3. Detailed per AC Execution

### AC-1: `<Title>`

**GIVEN:** `<from plan>`
**WHEN:** `<from plan>`
**THEN:** `<from plan>`
**Severity if FAILS:** `<from plan>`

#### Step-by-step executed:

| Step | Action taken | Driver | Evidence | Step verdict | Duration |
|---|---|---|---|---|---|
| 1 | Navigate to `URL` | Playwright `playwright_navigate` | [step-01-navigate.png](<rel path>) + [step-01-visible_text.txt](<rel path>) | PASS / FAIL | 3s |
| 2 | Click `<name>` | Playwright `playwright_click` | [step-02-click.png](<rel path>) | PASS / FAIL | 1s |
| 3 | Fill field: `<A>` = `<value>` | Playwright `playwright_fill` | [step-03-fill.png](<rel path>) | PASS / FAIL | 1s |
| ... | ... | ... | ... | ... | ... |
| N | Check expected text `"<XYZ>"` visible | Assertion `playwright_get_visible_text` | [FINAL_ASSERT_<short>.png](<rel path>) | PASS / FAIL | 2s |

**Final THEN assertion check:**
- ✅ PASS / ⚠️ PARTIAL / ❌ FAIL / ⏭️ SKIP
- `<notes: e.g. "All 3 THEN conditions matched visible_text. Screenshot FINAL_ASSERT shows success banner.">`
- `<if FAIL: "THEN-2 (URL /success) never reached; stuck on /checkout. FINAL_ASSERT shows cart still has items.">`

---

### AC-2: `<Title>`
*(same structure as AC-1)

---

## 4. Smoke Tests — §2 Plan Results

| # | Area | Check | Expected | Result | Evidence | Notes |
|---|---|---|---|---|---|---|
| S1 | Build | Build command completes | 0 errors | PASS / FAIL / SKIP | [SMOKE_S1_build.log](<rel path>) | |
| S2 | Lint | Lint/biome check | 0 errors, ≤5 warnings | PASS / FAIL / SKIP | [SMOKE_S2_lint.log](<rel path>) | |
| S3 | Login | User logs in with known credentials | Dashboard renders | PASS / FAIL / HUMAN_REVIEW | [SMOKE_S3_login.png](<rel path>) | |
| S4 | Nav | Top-level 3 pages load | No 500 / WSOD | PASS / FAIL / SKIP | [SMOKE_S4A_pageX.png](<rel path>) etc. | |
| S5 | Logs | Server logs during run | No CRITICAL / ERROR entries | PASS / FAIL / HUMAN_REVIEW | [SMOKE_S5_logs_grep.log](<rel path>) | |

---

## 5. Human-Only Items Remaining

These items from `§3 Security & Compliance Spot-checks from original plan MUST be verified by a human reviewer before PR merge. They were SKIPPED during auto-executor.

### 5.1 §3 Security/Compliance checklist (human reviewer):
- [ ] Review all env var references in diff: no secrets committed
- [ ] Review PII handling: no raw email / phone in logger output
- [ ] All new DB host URLs confirm dev/staging only (not prod RDS/supabase/neon)
- [ ] Destructive SQL (DROP/TRUNCATE/DELETE): NODE_ENV guard + explicit consent

### 5.2 Steps marked HUMAN_REVIEW_NEEDED during run:
| AC-ID / Step | Why skipped | Human reviewer to verify |
|---|---|---|
| AC-3 Step 6 | Check email sandbox / external service | Confirm `<external email arrived to: `<email>` / dashbaord>` |
| ... | ... | ... |

---

## 6. Rollback Plan

Reference: [§4 of [manual_test_plan.md §4 Rollback Plan](<link to plan#4-rollback-plan>)

If deployed and breaks:
```
<summary from plan>
```

---

## 7. Overall Verdict

**Overall: ✅ / ❌ / ⚠️**

- ✅ **PASS** → All BLOCKER and HIGH-severity ACs PASS. Ship-ready conditional on human-only items above being signed off.
- ❌ **FAIL** → ≥1 BLOCKER AC FAIL or ≥2 HIGH AC FAIL. NOT ship-ready.
- ⚠️ **CONDITIONAL PASS** → PARTIAL results + low-severity FAILs only; conditional on human review.

Single-sentence headline:
`<e.g. "All 4 BLOCKER/HIGH ACs PASS; 2 LOW AC-5 PARTIAL (email external sandbox HUMAN_REVIEW; overall CONDITIONAL on sandbox verified."

---

## 8. Sign-off

| Role | Who | Date | Comments |
|---|---|---|---|
| harness-manual-test-executor (auto) | Agent | `<version>` | `<iso datetime of report generated |
| Human verifies §3 & HUMAN_REVIEW items | — | — | |
| Final QA gate owner final sign-off | — | — | |
