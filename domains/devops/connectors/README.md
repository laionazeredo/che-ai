# Connectors — devops domain

> Placeholder folder.
>
> Official connectors to external tools for this domain.
>
> Each connector file: MCP server name + OFFICIAL CLI name + HARD RULE:
> **NEVER use manual raw HTTP curl/fetch. ALWAYS use the tool's official MCP server OR official CLI.**
>
> See engineering-contracts §18 (GitHub gh-only rule) for the pattern generalised to
> every external connector in §20 EXTERNAL CONNECTORS — ONLY OFFICIAL CLI/MCP.

To create a connector for this domain, copy a template from `domains/ux/connectors/figma.config.md` and adapt.
